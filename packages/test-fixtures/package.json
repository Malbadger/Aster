{
  "name": "@law/test-fixtures",
  "version": "0.1.0",
  "private": true,
  "description": "Deterministic scripted providers and fixtures. TEST-ONLY: never imported by production code paths.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@law/contracts": "0.1.0"
  },
  "devDependencies": {
    "typescript": "5.9.2",
    "vitest": "^3.2.6"
  }
}
