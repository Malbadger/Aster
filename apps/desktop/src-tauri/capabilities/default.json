{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Narrow default capabilities for the LAW main window. No broad filesystem, shell, or network permissions are granted to the webview; all such effects flow through lawd.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-set-title",
    "dialog:allow-open",
    "dialog:allow-save",
    "dialog:allow-message"
  ]
}
